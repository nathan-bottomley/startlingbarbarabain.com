<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">
    {% assign post = collections.podcastEpisode | getCollectionItem -%}
    {%- if pageDescription -%}
      {% assign metaDescription = pageDescription %}
    {%- elsif post -%}
      {% assign metaDescription = post.content | strip_html | strip | truncatewords: 30 %}
    {%- else -%}
      {% assign metaDescription = site.pageDescription %}
    {%- endif %}
    <meta name="description" content="{{ metaDescription }}">
    <script src="/js/dropdown-menu.js" type="module"></script>
    <link rel="alternate" type="application/rss+xml" title="{{ site.title }}" href="{{ site.url }}/feed/podcast" />
    <link rel="preload" href="/fonts/FuturaBlack.woff2" as="font" type="font/woff2" crossorigin>
    <link rel="preload" href="/fonts/FuturaBook.woff2" as="font" type="font/woff2" crossorigin>
    <link rel="stylesheet" href="/css/index.css">
    {% if includeAudioPlayer -%}
    <script type="module" src="https://cdn.jsdelivr.net/npm/media-chrome@4/+esm"></script>
    {%- endif %}
    <meta name="generator" content="{{ eleventy.generator }}">
    <meta name="apple-itunes-app" content="app-id=1714034909">
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png">
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png">
    <link rel="manifest" href="/site.webmanifest">
    <meta name="theme-color" content="#cfccc8">
    <meta property="og:type" content="website">
    <meta property="og:site_name" content="{{ site.title }}">
    <meta property="og:title" content="{{ pageTitle | default: site.title }}">
    <meta property="og:description" content="{{ metaDescription }}">
    {%- if post %}
    <meta property="og:image" content="{{ site.url }}/img/{{ post.data.imageFile }}">
    <meta property="og:image:alt" content="{{ post.data.imageAlt }}">
    {%- else %}
    <meta property="og:image" content="{{ site.url }}/img/link-logo.jpg">
    <meta property="og:image:alt" content="{{ site.linkLogoAltText }}">
    {%- endif %}
    <title>{{ pageTitle }}</title>
  </head>
  <body>
    <p class="strapline">A <em>Space: 1999</em> commentary podcast from the makers of
      <a href="https://bondfinger.com"><em>Bondfinger</em></a>.</p>
    <header>
      <div class="header-content">
      <img eleventy:ignore src="/img/barbara.png" alt="Head shot of Barbara Bain from the title sequence of Series 1 of Space: 1999. She looks impossible to startle.">
      <h1 class="site-title">
        <a href="/"><small>startling</small><br>Barbara Bain</a>
      </h1>
      </div>
    </header>
    <nav class="main-navigation">
      <ul>
        <li>
          <dropdown-menu>
            <button class="dropdown-menu-toggle-button" aria-haspopup="true">Subscribe</button>
            <ul class="dropdown-menu" role="menu">
              {% for subscription in podcast.subscriptions %}
              <li role="none"><a role="menuitem" href="{{ subscription.url }}">{{ subscription.name }}</a></li>
              {% endfor %}
            </ul>
          </dropdown-menu>
        </li>
        <li><a href="/podcasts">Our podcasts</a></li>
      </ul>
    </nav>
    <main>
    {{ content }}
    </main>
    <footer>
      <p>
      {{ podcast.copyrightNotice }}.
      Website created by <a href="https://gunsandfrocks.com">Nathan Bottomley</a>
      using <a href="https://11ty.dev">Eleventy</a> and <a href="https://eleventy-plugin-podcaster.com">Podcaster</a>.
      </p>
    </footer>
    <script src="https://tinylytics.app/embed/akwQjdcd9FjMn5YYPi6H.js" defer></script>
  </body>
</html>
