---
title: Startling Barbara Bain
permalink: "/{% if pagination.pageNumber > 0 %}page/{{ pagination.pageNumber | plus: 1 }}/{% endif %}"
layout: layouts/base
pagination:
  data: collections.post
  size: 12
  reverse: true
  alias: posts
linkInTitle: true
---
{% for post in posts %}
  {% include 'episode.liquid' %}
{% endfor %}
